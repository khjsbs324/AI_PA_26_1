name: Apply real checklist section

on:
  push:
    paths:
      - '.github/workflows/apply-real-checklist-section.yml'

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Replace pseudo checklist with real prompt card
        shell: bash
        run: |
          python3 - <<'PY'
          from pathlib import Path
          from html import escape

          prompt